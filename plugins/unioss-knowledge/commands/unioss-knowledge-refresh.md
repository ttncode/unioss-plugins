---
description: Crawl + distill the current window (daily|weekly|monthly|yearly) into the knowledge base.
argument-hint: [daily|weekly|monthly|yearly]
---

# UNIOSS Knowledge — Refresh

Invoke the `unioss-knowledge-refresh` skill with the requested cadence (default `daily`).
